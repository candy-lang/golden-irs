# HIR for module Examples:average
$use = { (non-fuzzable) $use:relativePath ->
  $use:importedModule = relative to module Examples:average, use $use:relativePath
}
$0 = error
  Examples:average (span: 0 – 324): The parser couldn't parse this rest.
$1 = []
