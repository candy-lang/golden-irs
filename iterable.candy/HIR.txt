# HIR for module Examples:iterable
$use = { (non-fuzzable) $use:relativePath ->
  $use:importedModule = relative to module Examples:iterable, use $use:relativePath
}
$0 = error
  Examples:iterable (span: 0 – 998): The parser couldn't parse this rest.
$1 = []
