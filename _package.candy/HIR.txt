# HIR for module Examples:_package
$use = { (non-fuzzable, but passes on responsibility) $use:relativePath ->
  $use:importedModule = relative to module Examples:_package, use $use:relativePath
}
$0 = "Builtins"
$1 = call $use with $0
$2 = Nothing
$3 = []
