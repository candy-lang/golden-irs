# HIR for module Examples:sqrt
$use = { (non-fuzzable) $use:relativePath ->
  $use:importedModule = relative to module Examples:sqrt, use $use:relativePath
}
$0 = error
  Examples:sqrt (span: 0 – 861): The parser couldn't parse this rest.
$1 = []
