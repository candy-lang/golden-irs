# HIR for module Examples:
$use = { (non-fuzzable) $use:relativePath ->
  $use:importedModule = relative to module Examples:, use $use:relativePath
}
$0 = error
  Examples: (span: 0 – 418): The parser couldn't parse this rest.
$1 = []
