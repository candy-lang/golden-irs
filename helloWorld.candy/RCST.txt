# RCST for module Examples:helloWorld
Cst { data: (), kind: Error { unparsable_input: "# A self-contained hello world example that doesn't even use the Core library.\n\nmain := { environment ->\n  environment.stdout \"Hello, world!\"\n}", error: UnparsedRest } }
Cst { data: (), kind: Newline("\n") }
