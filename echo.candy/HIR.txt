# HIR for module Examples:echo
$use = { (non-fuzzable) $use:relativePath ->
  $use:importedModule = relative to module Examples:echo, use $use:relativePath
}
$0 = error
  Examples:echo (span: 0 – 252): The parser couldn't parse this rest.
$1 = []
